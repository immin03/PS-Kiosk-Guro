/**
 * 파마스퀘어 브랜드 컬러.
 *
 * 브랜드 컬러 가이드에 실린 값만 씁니다. 화면에서 색을 직접 적지 말고 여기서
 * 가져다 쓰세요. 가이드 밖 색이 필요해지면 먼저 가이드를 고칩니다.
 *
 *   무채   #000000 · #10181E(BLACK 6 C) · #2D373D
 *          #CCCCCC · #D8E1E2(7541 C) · #D9E4E3(P 123-1 C) · #E5E5E5
 *   청록   #005251(7722 C) · #006666 · #00978F(7716 C) · #339999
 *          #01C0A4(922 C) · #2BCAB0(929 C) · #00C8B3 · #63CAC1
 *   초록   #33CC99 · #66CC99
 *
 * 가이드 밖 색은 아래 두 자리뿐이고, 이유를 각각 적어 두었습니다.
 */

export const BRAND = {
  primary: "#005251",     /* 7722 C — 헤드라인 · BI */
  secondary: "#00978F",   /* 7716 C — 본문 강조 */
  accent: "#01C0A4",      /* 922 C — 메인 · CTA · 활성 탭 */
  accentSoft: "#2BCAB0",  /* 929 C */
  light: "#63CAC1",
  tint: "#D9E4E3",        /* P 123-1 C — 배지 · 선택 배경 */

  /* 본문은 파마 블랙 라이트를 씁니다. BLACK 6 C 는 로고·인쇄용으로 남깁니다. */
  ink: "#2D373D",
  text: "#2D373D",
  muted: "#2D373D",   /* 가이드의 회색은 본문에 쓰기엔 밝습니다. 크기·굵기로 층을 가릅니다. */

  border: "#D8E1E2",      /* 7541 C */
  line: "#E5E5E5",
  bg: "#FFFFFF",
  surface: "#FFFFFF",
  surfaceSoft: "#E5E5E5",
};

/**
 * 지도 존 색.
 *
 * 가이드는 청록 한 계열이라 색상만으로 존을 다 가를 수 없습니다. 가이드 안에서
 * 밝기가 다른 값을 골라 이웃한 존끼리 붙지 않게 배치했고, 존 이름이 지도에 큰
 * 글씨로 얹히므로 색은 구역을 나누는 정도만 합니다.
 *
 * 펫만 예외입니다. 손님이 다른 목적으로 따로 찾아오는 구역이라 청록 계열에
 * 묻히면 안 됩니다. 가이드에 주황이 없어 이 한 자리를 밖에서 가져왔습니다.
 */
export const ZONE_COLOR = {
  "건기식": "#00978F",
  "브랜드존": "#005251",
  "뷰티": "#01C0A4",
  "라이프": "#63CAC1",
  "식품·음료": "#339999",
  "프로모션": "#66CC99",   /* 이벤트존 — 가이드에서 가장 밝은 초록 */
  "펫": "#FF9F43",        /* 가이드 밖 — 위 설명 참고 */
  "기타": "#CCCCCC",
};

/**
 * 모서리.
 *
 * 지금 화면에는 3·6·8·10·12·14·16·20·40 이 섞여 있었습니다. 네 가지로 줄입니다.
 * 큰 값은 요소를 물렁해 보이게 합니다 — 버튼은 sm 을 씁니다.
 */
export const RADIUS = {
  xs: 4,     /* 배지 · 지도 칸 */
  sm: 10,    /* 버튼 · 입력칸 · CTA */
  md: 14,    /* 카드 */
  pill: 999, /* 토글 · 점 */
};

export const ZONE_FALLBACK = ZONE_COLOR["기타"];

/**
 * 현재 위치 — 가이드 밖 색입니다.
 * 손님이 자기 자리를 찾는 표시라 청록 계열과 섞이면 쓸모가 없습니다.
 * 가이드에 붉은 계열이 없어 이 한 자리만 예외로 둡니다.
 */
export const YOU_HERE = "#F04438";
