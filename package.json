{
  "name": "ps-kiosk-guro",
  "private": true,
  "version": "8.0.0",
  "type": "module",
  "description": "파마스퀘어 구로점 키오스크 앱",
  "scripts": {
    "catalog": "node scripts/publish-catalog.mjs",
    "predev": "npm run catalog",
    "dev": "vite",
    "prebuild": "npm run catalog",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "sync:racks": "node scripts/sync-racks.mjs && node scripts/publish-catalog.mjs",
    "check:racks": "node scripts/sync-racks.mjs --check"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
